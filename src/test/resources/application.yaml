spring:
  datasource:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    url: jdbc:tc:mysql:8.0.32:///search_location?TC_INITSCRIPT=file:schema/ddl.sql
logging:
  level:
    org:
      springframework:
        jdbc:
          core: TRACE
